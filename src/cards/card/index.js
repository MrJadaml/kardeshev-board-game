const Card = ({ data}) => {
  return (
    <div>
      <div className="card">

      </div>
      <div>
        <button onClick={() => {}}>Play</button>
        <button onClick={() => {}}>Discard</button>
      </div>
    </div>
  );
}


export default Card;
